package com.bitdreamit.astm.asyncastm.service.states.callback;

public enum AstmConnectionStatus {
   CONNECTING,
   DISCONNECTING,
   EXITING,
   IDLE,
   RECONNECTING,
   STARTING,
   RECEIVING,
   SENDING,
   ERROR;
}
